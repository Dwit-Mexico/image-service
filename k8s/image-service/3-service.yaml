apiVersion: v1
kind: Service
metadata:
  name: image-service
  namespace: production
spec:
  selector:
    app: image-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
